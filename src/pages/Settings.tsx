import Layout from '../Layouts/Layout';

const Home = () => {
  return (
    <Layout containerClassName='flex-grow flex flex-col justify-center items-center '>
      <h2 className='mb-2 text-4xl font-semibold'>Settings Page</h2>
    </Layout>
  );
};

export default Home;
